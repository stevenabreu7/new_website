import styled from 'styled-components';
import tw from 'tailwind.macro';

export const Image = styled.figure`
  ${tw`w-24 float-left p-3`};
`;

export const ThisContainer = styled.div`
  ${tw`max-w-screen-md mx-auto`};
`;